{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others --success first \"npm run start:dev --prefix ./packages/api\" \"npm run serve --prefix ./packages/app\"",
    "build": "lerna run build"
  },
  "devDependencies": {
    "concurrently": "^6.1.0",
    "lerna": "^4.0.0"
  }
}
